[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers --html=report.html --self-contained-html
markers =
    smoke: core purchase journey, must pass before anything else is trusted
    broken_user: runs against SauceDemo accounts that are seeded with defects
